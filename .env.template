# required if you want llm observability, authentication
HELICONE_CONTROL_PLANE_API_KEY="sk-helicone-..."

# Provider credentials — one env var per slot in config/embedded/credentials.yaml.
# Naming: AI_GATEWAY_CREDENTIAL_<ID> where id uses underscores (gemini-free → GEMINI_FREE).
# Missing secret → slot skipped at startup. Legacy {PROVIDER}_API_KEY still works for *-default slots.

AI_GATEWAY_CREDENTIAL_OPENAI_DEFAULT="sk-proj-..."
AI_GATEWAY_CREDENTIAL_ANTHROPIC_DEFAULT="sk-..."
AI_GATEWAY_CREDENTIAL_GEMINI_FREE="..."
AI_GATEWAY_CREDENTIAL_GEMINI_FREE_2="..."
AI_GATEWAY_CREDENTIAL_GEMINI_FREE_3="..."
AI_GATEWAY_CREDENTIAL_GEMINI_FREE_4="..."
AI_GATEWAY_CREDENTIAL_GEMINI_DEFAULT="..."
AI_GATEWAY_CREDENTIAL_GROQ_DEFAULT="gsk_..."
AI_GATEWAY_CREDENTIAL_OPENROUTER_DEFAULT="sk-or-..."
# Cloudflare: account_id:api_token in one value
AI_GATEWAY_CREDENTIAL_CLOUDFLARE_DEFAULT="account_id:cfut_..."
AI_GATEWAY_CREDENTIAL_CEREBRAS_DEFAULT="..."
AI_GATEWAY_CREDENTIAL_MISTRAL_DEFAULT="..."
AI_GATEWAY_CREDENTIAL_OPENCODE_DEFAULT="..."
# GitHub Models: PAT with models:read scope
AI_GATEWAY_CREDENTIAL_GITHUB_MODELS_DEFAULT="ghp_..."

# ChatGPT web session (not AI_GATEWAY_CREDENTIAL_* — file path)
# CHATGPT_BROWSER_CLI=dev/session.json

# DeepSeek web — session file path (not an API key).
# DEEPSEEK_BROWSER_CLI=dev/deepseek-session.json
# AI_GATEWAY_CREDENTIAL_DEEPSEEK_WEB_DEFAULT=dev/deepseek-session.json

# Perplexity web — session file path per account slot (dormant; crate retained).
# Default account: PERPLEXITY_BROWSER_CLI and AI_GATEWAY_CREDENTIAL_PERPLEXITY_WEB_DEFAULT
# PERPLEXITY_BROWSER_CLI=dev/perplexity-session.json
# AI_GATEWAY_CREDENTIAL_PERPLEXITY_WEB_DEFAULT=dev/perplexity-session.json
# Second account example:
# AI_GATEWAY_CREDENTIAL_PERPLEXITY_WEB_ALT=dev/perplexity-alt.json

# Legacy aliases (optional fallback if universal vars are unset):
# OPENAI_API_KEY=...
# GEMINI_FREE_TIER_APIKEY=...
# CLOUDFLARE_API_KEY_WITH_ACCOUNT_ID=account_id:cfut_...
# or split Cloudflare:
# CLOUDFLARE_ACCOUNT_ID="..."
# CLOUDFLARE_API_KEY="..."

AWS_ACCESS_KEY="asbbj340j"
AWS_SECRET_KEY="kfjd09431"

OTEL_METRIC_EXPORT_INTERVAL=10000
